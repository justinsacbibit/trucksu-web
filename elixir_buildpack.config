pre_compile="sudo -s -- <<EOF apt-get -y install php5-cli php5-mcrypt && cat /etc/php5/cli/php.ini | grep 'extension=mcrypt.so' > /dev/null && echo 'Ayy lmao' > /dev/null || echo 'extension=mcrypt.so' >> /etc/php5/cli/php.ini EOF"
post_compile="mix ecto.migrate"
